VITE_API_URL=http://localhost:3000

# Runtime attestation verifier tuning. Usually leave unset so the frontend
# fetches runtime keys from /api/eiaa/v1/runtime/keys and refreshes every 10 min.
# VITE_RUNTIME_KEY_TTL_MS=600000
# VITE_RUNTIME_PUBKEY_PEM=
